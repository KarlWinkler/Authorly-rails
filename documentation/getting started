for ubuntu 20.22.4

ruby version 3.0.0
rails version 7.0.3

use postgres databse -- version?

clone the repository at https://github.com/KarlWinkler/Authorly-rails

run `bundle`

start the server with `rails s`

* extras

pry for debugging
guard to run rspec unit tests

---

useful commands to remember

`bundle`
`rails s`
`rails c`
`sudo -u postgres psql -p 5433`
`guard`
`rails routes`